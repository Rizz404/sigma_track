import 'package:flutter/material.dart';

class AdminListScanLogsScreen extends StatelessWidget {
  const AdminListScanLogsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Admin List Scan Logs Screen')),
    );
  }
}
