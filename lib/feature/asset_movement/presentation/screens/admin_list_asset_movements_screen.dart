import 'package:flutter/material.dart';

class AdminListAssetMovementsScreen extends StatelessWidget {
  const AdminListAssetMovementsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Admin List Asset Movements Screen')),
    );
  }
}
