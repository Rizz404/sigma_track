name: Build Android APK

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.1'
          channel: 'stable'
          cache: true

      - name: Get dependencies
        run: flutter pub get

      - name: Build APK Release (fat APK)
        run: flutter build apk --release

      - name: Build APK Split per ABI
        run: flutter build apk --release --split-per-abi

      # Upload satu per satu biar nggak di-zip otomatis sama GitHub
      - name: Upload Release APK (fat)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-fat
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Upload APK (armeabi-v7a)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-armeabi-v7a
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk

      - name: Upload APK (arm64-v8a)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-arm64-v8a
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk

      - name: Upload APK (x86_64)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-x86_64
          path: build/app/outputs/flutter-apk/app-x86_64-release.apk

  # Optional: Create GitHub Release when tag is pushed
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          # Karena nggak specify 'name', dia bakal download semua artifact
          # ke folder masing-masing sesuai nama artifact di atas.

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          # Nah, path-nya sesuain sama folder hasil download tadi
          files: |
            artifacts/app-release-fat/app-release.apk
            artifacts/app-release-armeabi-v7a/app-armeabi-v7a-release.apk
            artifacts/app-release-arm64-v8a/app-arm64-v8a-release.apk
            artifacts/app-release-x86_64/app-x86_64-release.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
